on:
  push:
    branches:
      - main

### Job Definition
jobs:
  test:
    runs-on: ubuntu-latest

### Job Steps
steps:
  - name: Checkout Code
    uses: actions/checkout@v2

  - name: Run a Test Command
    run: echo "Hello, World!"